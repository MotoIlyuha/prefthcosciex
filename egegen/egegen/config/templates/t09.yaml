# Задание 9 — обработка чисел в электронной таблице.
task_no: 9
title: "Обработка чисел в электронных таблицах"

subtypes:
  # плейсхолдеры: {rows} {columns} {condition} {preview}
  "9.1_sums":
    title: "Условия на суммы наименьших и наибольших"
    difficulty: [1, 5]
    target_seconds: 360
  "9.2_one_repeat":
    title: "Ровно одно число повторяется дважды"
    difficulty: [2, 5]
    target_seconds: 380
  "9.3_all_distinct":
    title: "Все числа различны"
    difficulty: [1, 3]
    target_seconds: 340
  "9.4_divisible":
    title: "Делимость и чётность"
    difficulty: [1, 4]
    target_seconds: 350
  "9.5_average":
    title: "Среднее арифметическое и максимум"
    difficulty: [3, 5]
    target_seconds: 380

templates:
  - id: t9_sums
    subtype: "9.1_sums"
    text: |
      Откройте файл `9.csv` (или `9.ods`) — электронную таблицу,
      содержащую {rows} строк по {columns} натуральных чисел в каждой.

      Начало таблицы:

      {preview}

      Определите **количество строк** таблицы, в которых {condition}.

      В ответе запишите целое число.

  - id: t9_repeat
    subtype: "9.2_one_repeat"
    text: |
      Откройте файл `9.csv` (или `9.ods`), содержащий {rows} строк
      по {columns} натуральных чисел в каждой.

      Начало таблицы:

      {preview}

      Определите количество строк, в которых {condition}.

      В ответе запишите целое число.

  - id: t9_distinct
    subtype: "9.3_all_distinct"
    text: |
      Откройте файл `9.csv` (или `9.ods`) — таблицу из {rows} строк
      по {columns} натуральных чисел.

      Начало таблицы:

      {preview}

      Определите количество строк, в которых {condition}.

      В ответе запишите целое число.

  - id: t9_divisible
    subtype: "9.4_divisible"
    text: |
      Откройте файл `9.csv` (или `9.ods`), содержащий {rows} строк
      по {columns} натуральных чисел в каждой.

      Начало таблицы:

      {preview}

      Определите количество строк, в которых {condition}.

      В ответе запишите целое число.

  - id: t9_average
    subtype: "9.5_average"
    text: |
      Откройте файл `9.csv` (или `9.ods`) — таблицу из {rows} строк
      по {columns} натуральных чисел.

      Начало таблицы:

      {preview}

      Определите количество строк, в которых {condition}.

      В ответе запишите целое число.
