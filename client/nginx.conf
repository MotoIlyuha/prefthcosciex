server {
    listen 80;
    root /usr/share/nginx/html;

    # Hashed assets never change; Pyodide is versioned by its directory.
    location /assets/ { expires 1y; add_header Cache-Control "public, immutable"; }
    location /pyodide/ {
        expires 30d;
        types { application/wasm wasm; application/javascript mjs js; application/json json; application/zip zip; }
    }
    location = /sw.js { add_header Cache-Control "no-cache"; }
    location / {
        try_files $uri /index.html;
        add_header Cache-Control "no-cache";
    }
}
