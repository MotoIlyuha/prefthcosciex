# Известные пробелы

Честный список того, что не сделано или проверено не полностью, и почему.

## Блокеры, не зависящие от кода

| # | Пробел | Причина | Что нужно |
|---|---|---|---|
| 1 | Стенд не развёрнут, нет публичного URL | нет сервера и SSH‑доступа (раздел 0, п. б промпта) | секреты `STAGE_SSH_*` в GitHub; деплой запустится сам (`deploy-stage.yml`) |
| 2 | Бот и Mini App не проверены в живом Telegram | нет токена бота (раздел 0, п. а) | `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`; затем шаги BotFather из RUNBOOK |
| 3 | Юридические тексты — черновики | нужна консультация юриста (14.4) | проверка `docs/LEGAL/*`, уведомление РКН |
| 4 | Форматы 10, 13, 23, 27 — по проекту демоверсии | утверждённая демоверсия выйдет в ноябре 2026 | сверка и правка `fipi_2027.yaml` (RUNBOOK, раздел 6) |

## Проверено не до конца

| # | Что | Как проверено | Чего не хватает |
|---|---|---|---|
| 5 | Вход через Telegram Login Widget (redirect без стороннего скрипта) | HMAC на сервере — тесты; разбор `tgAuthResult` — юнит‑тест | прогон на реальном `oauth.telegram.org` (нужен бот и домен) |
| 6 | Нагрузка 500 одновременных пользователей | k6 локально: 25 пользователей, p95 17 мс | прогон на стенде (`make load` с `VUS=500`) |
| 7 | Образы раннера и бэкапа | сборка в CI (job `images`), там же дамп доходит до S3; раннер — 30 тестов в nsjail локально и в CI | локальная сборка в среде разработки невозможна (прокси с подменой TLS, репозиторий Alpine закрыт) |
| 8 | PostHog | отправка реализована, по умолчанию события пишутся в таблицу `events` | проверка с реальным проектом PostHog |

## Упрощения

| # | Что | Решение |
|---|---|---|
| 9 | Лимит памяти 256 МБ в браузерном Python | в браузере ограничено только время (10 с); память ограничивает серверный раннер |
| 10 | Недельный отчёт куратору в PDF | HTML‑страница для печати (D‑029) |
| 11 | Стрелки тренда уверенности | считаются по ежедневным снимкам; первую неделю показывают «→» |
| 12 | Монетизация (12.8) и запасные площадки VK/MAX (11.10) | в дизайн‑доке помечены как необязательные; не реализованы |
| 13 | Интерфейс только на русском | целевая аудитория — школьники РФ |
| 14 | Лига группы показывает ученикам имена одногруппников | включает только репетитор; для родителей недоступна (4.7) |
